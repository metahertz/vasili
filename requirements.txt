flask
flask-socketio
pymongo
speedtest-cli
netifaces2
python-iptables
pyyaml
pyarchops-dnsmasq
requests
cryptography
# Headless-browser captive-portal fallback (JS-only / tickbox portals).
# After installing, run `playwright install chromium` (plus arm64 system deps
# on the Pi). The browser path degrades gracefully if Playwright/Chromium are
# absent — the lightweight HTTP path still governs.
playwright
# D-Bus + GLib bindings. Used by the BLE control interface (ble_peripheral.py,
# talks to BlueZ over the system bus) and by ConnectionMonitor's push-based
# D-Bus StateChanged path (falls back to nmcli polling if unavailable).
# On the Pi, deploy.sh installs the distro builds (python3-dbus / python3-gi)
# and makes the venv --system-site-packages, so pip finds these already
# satisfied rather than compiling them from source. Listed here so a plain
# `pip install -r` on a dev box still pulls them when building wheels is viable.
dbus-python
PyGObject
